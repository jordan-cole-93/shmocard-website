// PostCSS config for Tailwind 4. The @tailwindcss/postcss plugin is the
// canonical Tailwind 4 entry — replaces the legacy `tailwindcss` + `autoprefixer`
// pair from v3 setups.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;

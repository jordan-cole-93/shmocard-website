---
phase: 260507-d4g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/rules/subagent-dispatch.md
  - .claude/rules/skill-routing.md
  - .claude/rules/design-system.md
autonomous: true
requirements:
  - QUICK-01-trim-claude-md
  - QUICK-02-extract-subagent-dispatch-rule
  - QUICK-03-extract-skill-routing-rule
  - QUICK-04-update-design-system-precedence

must_haves:
  truths:
    - "CLAUDE.md is between 45 and 55 lines (router, not encyclopedia)"
    - "No content removed from CLAUDE.md is lost — every paragraph lives in a rule file"
    - "CLAUDE.md no longer contains the SDK-issue-#102 rationale, the wrappers table, the LAYOUT IS LOCKED paragraph, or the multi-row parent-agent skill routing table"
    - ".claude/rules/subagent-dispatch.md exists and contains SDK-issue-#102 rationale + shmocard-shopify-work usage + UI hand-author guardrails pattern + LAYOUT IS LOCKED paragraph"
    - ".claude/rules/skill-routing.md exists and contains the parent-agent skill routing table + the design/UI hard rule"
    - "All references in CLAUDE.md still resolve — no links to removed skills (shmocard-polish-section, shmocard-build-page, shmocard-design-review, gsd-shmocard, etc.)"
    - "design-system.md precedence table includes a row for subagent-dispatch.md"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Lean router pointing at rules + skills + context"
      contains: "Where to find things, vault context, live store rule, current phase"
    - path: ".claude/rules/subagent-dispatch.md"
      provides: "Subagent dispatch rules — SDK limitation, Shopify wrapper, UI hand-author guardrails, layout lock"
      contains: "SDK issue #102"
    - path: ".claude/rules/skill-routing.md"
      provides: "Parent-agent skill routing reference (frontend-design, impeccable, gsd-progress, etc.)"
      contains: "frontend-design"
    - path: ".claude/rules/design-system.md"
      provides: "Existing design-system rule — precedence table updated"
      contains: "subagent-dispatch.md"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/rules/subagent-dispatch.md"
      via: "explicit reference in rules pointer table"
      pattern: "subagent-dispatch"
    - from: "CLAUDE.md"
      to: ".claude/rules/skill-routing.md"
      via: "explicit reference in rules pointer table"
      pattern: "skill-routing"
---

<objective>
Trim CLAUDE.md from 78 lines to ~50 lines (router, not encyclopedia per Mark Kashef). Relocate three offloadable blocks into two new rule files: `.claude/rules/subagent-dispatch.md` and `.claude/rules/skill-routing.md`. Update `.claude/rules/design-system.md` precedence table to reference the new dispatch rule.

Purpose: CLAUDE.md should auto-load as a thin pointer file. Detailed routing tables, SDK rationale, and procedural rules belong in dedicated rule files that get loaded contextually.
Output: Slimmer CLAUDE.md + 2 new rule files + 1 updated rule file. Pure docs refactor — no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/design-system.md
@.claude/rules/file-organization.md
@.claude/rules/live-store-protection.md
@.claude/rules/verification.md
@.claude/rules/vault-conventions.md
@.claude/rules/shopify-data-discipline.md

<interfaces>
<!-- Existing project skills (only these two exist — do NOT reference removed ones) -->
- `.claude/skills/shmocard-shopify-work/` — wrapper for Shopify subagent dispatch
- `.claude/skills/shopify-data-discipline/` — auto-trigger on product/cart/price prompts

<!-- Removed skills (DO NOT reference in any new file) -->
- shmocard-polish-section, shmocard-build-page, shmocard-design-review (UI wrappers — deleted)
- gsd-shmocard, caveman-shmocard, impeccable-shmocard, build-shmocard-component (deleted)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subagent-dispatch.md and skill-routing.md rule files</name>
  <files>.claude/rules/subagent-dispatch.md, .claude/rules/skill-routing.md</files>
  <action>
Create two new rule files. Both must be self-contained and read naturally as standalone rules.

**File A: `.claude/rules/subagent-dispatch.md`**

Structure:
1. Title: `# Subagent Dispatch`
2. Lead paragraph (1-2 sentences) — when this rule applies: any time the parent agent spawns a subagent via the Agent tool.
3. Section `## Why wrappers exist` — the SDK-issue-#102 rationale paragraph from current CLAUDE.md line 46. Spawned subagents structurally cannot access the Skill tool. Global skills (`frontend-design`, `impeccable`, `redesign-skill`) only work inside the parent agent. Rules must travel as text inside the Agent prompt.
4. Section `## Shopify subagent dispatch` — table with single row: `shmocard-shopify-work` wrapper for Storefront / cart / Server Action / webhook work. State: ALWAYS invoke this wrapper first; never write the Shopify subagent prompt freehand. Live-store-protection enforcement.
5. Section `## UI subagent dispatch (hand-authored guardrails)` — explain that no UI wrapper skill exists anymore; the parent must hand-author guardrails directly into the Agent prompt. Required steps: (a) read `.claude/rules/design-system.md` first, (b) inline its hard rules into the Agent prompt verbatim, (c) inline the LAYOUT IS LOCKED paragraph (below) when the task is polish-flavored.
6. Section `## LAYOUT IS LOCKED on polish tasks` — the paragraph from current CLAUDE.md line 69. When Jordan says "polish", "fix the spacing", "the design is missing something", or any refinement-not-restructure language: NEVER change grid columns, element ordering, tile size ratios, or structural HTML. Polish = spacing / type / color / mascot only. See `.claude/rules/design-system.md` for full rule text.
7. Section `## Hard rule` — single sentence: when dispatching ANY subagent that touches UI or Shopify, this rule must be honored before the Agent tool is invoked.

**File B: `.claude/rules/skill-routing.md`**

Structure:
1. Title: `# Skill Routing (Parent Agent)`
2. Lead paragraph — applies when the parent agent decides which skill to load before acting. Subagent dispatch lives in `subagent-dispatch.md`.
3. The parent-agent skill routing table from current CLAUDE.md lines 56-65 (8 rows: frontend-design, impeccable, redesign-skill, gsd-progress, /gsd-fast, /gsd-resume-work, claude-mem, context-mode).
4. Section `## Hard rule for design / UI prompts` — the paragraph from current CLAUDE.md line 67. For any prompt mentioning design / layout / UI / component / hero / section / typography / palette / animation: load `frontend-design` first AND read `.claude/rules/design-system.md`. Design-system rules WIN on visual / typography / mascot / section-rotation / utility-class-prefix conflicts. `frontend-design` anti-slop principles still apply for composition / hierarchy / cognitive load.

**Both files use the same heading style and tone as existing rule files** (`design-system.md`, `verification.md`). Imperative, terse, concrete.

Do NOT reference any removed skills (shmocard-polish-section, shmocard-build-page, shmocard-design-review, gsd-shmocard, caveman-shmocard, impeccable-shmocard, build-shmocard-component). The only two project skills that exist are `shmocard-shopify-work` and `shopify-data-discipline`.
  </action>
  <verify>
    <automated>test -f .claude/rules/subagent-dispatch.md && test -f .claude/rules/skill-routing.md && grep -q "SDK issue #102\|sdk-typescript/issues/102" .claude/rules/subagent-dispatch.md && grep -q "shmocard-shopify-work" .claude/rules/subagent-dispatch.md && grep -q "LAYOUT IS LOCKED" .claude/rules/subagent-dispatch.md && grep -q "frontend-design" .claude/rules/skill-routing.md && ! grep -q "shmocard-polish-section\|shmocard-build-page\|shmocard-design-review\|gsd-shmocard" .claude/rules/subagent-dispatch.md .claude/rules/skill-routing.md</automated>
  </verify>
  <done>
Both rule files exist. subagent-dispatch.md contains the SDK rationale, the shmocard-shopify-work usage instruction, the UI hand-author-guardrails pattern, and the LAYOUT IS LOCKED paragraph. skill-routing.md contains the parent-agent routing table and the design/UI hard rule. Neither file references removed skills.
  </done>
</task>

<task type="auto">
  <name>Task 2: Trim CLAUDE.md to ~50 lines and update design-system.md precedence table</name>
  <files>CLAUDE.md, .claude/rules/design-system.md</files>
  <action>
**Step A: Rewrite CLAUDE.md as a lean router (~45-55 lines).**

Target structure:

```markdown
# Shmocard Website

Brand website for Shmocard at `shmocard.com`. Retail front door — not an ad funnel. Headless Shopify + Next.js.

**Stack:** Next.js (App Router) + TypeScript · Tailwind CSS 4 · Vercel · Shopify Headless (pending). Full details in `context/general/backend.md`.

> **Status (2026-05-07):** Phase 1 + Phase 2 complete. Site is a bare Next.js + Tailwind shell with the design system audited and integration locked. Ready for Phase 3 rebuild — see `.planning/phases/02-design-system-review/TRANSLATION.md`.

---

## Where to find things

| Need | Read |
|---|---|
| Build status & roadmap | `context/general/scope.md` |
| Last session context | `context/general/handoff.md` |
| Tech stack details (Shopify, env vars, Vercel) | `context/general/backend.md` |
| Tools (skills, commands, hooks, MCPs) | `context/general/tools.md` |
| Project context (business, thesis, sub-brands) | `context/general/context.md` |
| Voice, copy, headlines, angles | `context/general/marketing.md` |
| Product catalog, formats, status | `context/general/product.md` |
| Per-page wireframes | `context/brainstorming/*.md` (pending) |
| Design system source | `context/design-system/` (read `SKILL.md` first) |
| Personal preferences | `CLAUDE.local.md` (gitignored) |

## Engineering rules (`.claude/rules/`)

| File | Applies when |
|---|---|
| `design-system.md` | Any UI / visual / design / component / layout / page work |
| `subagent-dispatch.md` | Spawning a subagent (UI hand-author guardrails or `shmocard-shopify-work` wrapper) |
| `skill-routing.md` | Parent agent deciding which skill to load |
| `live-store-protection.md` | Any Shopify Admin / theme / domain / payments touchpoint |
| `shopify-data-discipline.md` | Product data, prices, SKUs, cart, checkout |
| `verification.md` | Before claiming "done" on any task |
| `file-organization.md` | Adding / moving folders or top-level files |
| `vault-conventions.md` | Reading or writing to Jordan's Brain (Obsidian) |

## Vault context

**Base path:** `/Users/jordancole/Documents/Developement/Jordan's Brain/Projects/Shmocard/Shmocard Website/`

Read freely. Don't write except via the `vault-sync` skill. Don't duplicate vault notes into the repo.

## The one rule that supersedes everything

**Don't break the live store.** `shop.shmocard.com` is live. Never touch the primary Online Store theme, payment settings, or domain configuration. See `.claude/rules/live-store-protection.md`.

## Current phase

1. ✅ **Phase 1 — Docs refresh.**
2. ✅ **Phase 2 — Design system review.** Complete 2026-05-07.
3. **Phase 3 — Rebuild (next).** Tokens → base layout → homepage → `/shmo-review` → 3 PDPs → cart + Storefront API. Run `/gsd-plan-phase 3`.
4. **Phase 4 — Launch readiness.** Mobile pass, a11y, Vercel env, DNS cutover.

See `context/general/scope.md` for the live punch list and `.planning/ROADMAP.md` for atomic-commit history.
```

Verify final line count is between 45 and 55. Adjust by tightening or expanding the status sentence if needed — DO NOT re-introduce removed content.

Removed content (must NOT appear in new CLAUDE.md):
- The full "Auto-trigger skill routing" section heading and intro paragraph (lines 40-42)
- The "Subagent-dispatch wrappers" subsection including SDK-issue-#102 rationale, table, and hard rule (lines 44-52)
- The "Parent-agent skill routing" subsection including the 8-row table (lines 54-65)
- The "Hard rule" paragraph for design/UI prompts (line 67)
- The "LAYOUT IS LOCKED on polish tasks" paragraph (line 69)
- The wordy status footnote about 28 preview cards / 83 redundant cuts / Zustand decision detail (compressed into one sentence above)

**Step B: Update `.claude/rules/design-system.md` precedence table.**

Locate the `## Precedence with other rules` section (near end of file). Add a new row to the table:

```
| `subagent-dispatch.md` | **`subagent-dispatch.md` wins** for which wrapper to invoke and how to inline guardrails into Agent prompts. Design-system rules still govern the *content* of the UI guardrails. |
```

Insert this row after the `frontend-design` row, before the `live-store-protection.md` row (or wherever fits the existing alphabetical / logical order — match existing pattern).

Use Edit tool to make a single precise insertion. Do not rewrite the rest of design-system.md.
  </action>
  <verify>
    <automated>LINES=$(wc -l < CLAUDE.md | tr -d ' ') && [ "$LINES" -ge 45 ] && [ "$LINES" -le 60 ] && ! grep -q "SDK issue #102\|sdk-typescript/issues/102" CLAUDE.md && ! grep -q "LAYOUT IS LOCKED" CLAUDE.md && ! grep -q "shmocard-polish-section\|shmocard-build-page\|shmocard-design-review\|gsd-shmocard" CLAUDE.md && grep -q "subagent-dispatch.md" CLAUDE.md && grep -q "skill-routing.md" CLAUDE.md && grep -q "subagent-dispatch.md" .claude/rules/design-system.md</automated>
  </verify>
  <done>
CLAUDE.md is 45-60 lines, contains the rules-pointer table referencing subagent-dispatch.md and skill-routing.md, and no longer contains any of the removed paragraphs/tables. design-system.md precedence table includes a row for subagent-dispatch.md. All other rule files unchanged. No references to deleted skills survive in CLAUDE.md.
  </done>
</task>

</tasks>

<verification>
Final pass:
1. `wc -l CLAUDE.md` returns 45-60.
2. `grep -rn "shmocard-polish-section\|shmocard-build-page\|shmocard-design-review\|gsd-shmocard\|caveman-shmocard\|impeccable-shmocard\|build-shmocard-component" CLAUDE.md .claude/rules/subagent-dispatch.md .claude/rules/skill-routing.md` returns no matches.
3. Every removed paragraph from old CLAUDE.md is locatable via grep in one of the new rule files.
4. `ls .claude/rules/` shows: design-system.md, file-organization.md, live-store-protection.md, shopify-data-discipline.md, skill-routing.md, subagent-dispatch.md, vault-conventions.md, verification.md (8 files total).
</verification>

<success_criteria>
- CLAUDE.md is a lean router (45-60 lines).
- `.claude/rules/subagent-dispatch.md` and `.claude/rules/skill-routing.md` exist with the relocated content intact.
- `.claude/rules/design-system.md` precedence table references `subagent-dispatch.md`.
- No content lost; no broken references; no references to deleted skills.
- Pure docs refactor — no `.tsx`, `.css`, or other code changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260507-d4g-trim-claude-md-to-50-lines-and-move-wrap/260507-d4g-01-SUMMARY.md` documenting:
- Final CLAUDE.md line count
- Sections relocated (with source line numbers from old CLAUDE.md → destination file)
- The precedence-table row added to design-system.md
</output>

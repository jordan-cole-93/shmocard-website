---
phase: 03-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - app/page.tsx
  - components/home/home.css
  - components/home/home-data.ts
  - components/home/Hero.tsx
  - components/home/HeroTypeCycle.tsx
  - components/home/AudienceStrip.tsx
  - components/home/Proof.tsx
  - components/home/SubBrandSpotlight.tsx
  - components/home/CrewStrip.tsx
  - components/home/HowItWorks.tsx
  - components/home/VideoTestimonials.tsx
  - components/home/Compatibility.tsx
  - components/home/HomeFaq.tsx
  - components/home/FaqItem.tsx
  - components/home/FinalCta.tsx
  - components/home/Reveal.tsx
  - pictures/screenshots/03-03-homepage-full.png
  - pictures/screenshots/03-03-homepage-hero.png
autonomous: true
requirements: [REQ-01, REQ-09]

must_haves:
  truths:
    - "Homepage renders all 11 sections in the order locked by TRANSLATION.md"
    - "Hero displays locked headline 'The toolkit your crew\\'s been [missing | asking for].' with em type-cycle that does NOT cause layout shift"
    - "Section background rotation is marsh -> graham -> marsh -> ember -> marsh -> cocoa across the full page (per CONTEXT.md B6 + REQ-01)"
    - "All four sub-brands (Shmo Review, Shmo Biz, Shmo Link, Shmo Reputation) appear with equal visual weight"
    - "Shmo Review CTA links to /shmo-review; other three sub-brand CTAs trigger waitlist modal (modal stub OK; full wiring in 03-10)"
    - "Audience marquee scrolls left, 38s duration, paused on hover, 10 audiences per CONTEXT B2"
    - "ZERO occurrences of 'Pawn Leads' / 'pawnleads' / '@pawnleads' anywhere in source, alt text, filenames, or comments"
    - "Mascot pose budget respected: 4 hero-size mascots (200px) on sub-brand spotlights via showcase exception, 1 sticker on FAQ"
  artifacts:
    - path: app/page.tsx
      provides: "Homepage composition; server component"
    - path: components/home/home.css
      provides: "LAYOUT-only CSS (grids, padding, aspect ratios) for home sections"
    - path: components/home/home-data.ts
      provides: "SUB_BRANDS, AUDIENCES, SHOPS, QUOTES, HOW_STEPS, FAQ constants — content only, no JSX"
      exports: ["SUB_BRANDS", "AUDIENCES", "SHOPS", "QUOTES", "HOW_STEPS", "FAQ"]
    - path: components/home/Hero.tsx
      provides: "Hero section with type-cycle em + ember accent"
    - path: components/home/HeroTypeCycle.tsx
      provides: "'use client' type-cycle word swapper"
    - path: components/home/Reveal.tsx
      provides: "framer-motion shell for whileInView reveals"
  key_links:
    - from: components/home/Hero.tsx
      to: components/home/HeroTypeCycle.tsx
      via: client child swapping the em accent words
      pattern: 'HeroTypeCycle'
    - from: components/home/SubBrandSpotlight.tsx
      to: components/modals/WaitlistModal (plan 03-10)
      via: trigger button dispatches modal open
      pattern: 'data-waitlist|openWaitlist'
---

<objective>
Build the homepage by composing 11 sections from TRANSLATION.md against the canonical reference at `.claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/Shmocard Homepage.html` + `home.css` + `home-data.jsx`. Deliver REQ-01 (homepage with all 4 sub-brands equal weight, locked headline, section rotation) and REQ-09 (design system invariants).

Purpose: Marketing front door. Single click to /shmo-review for buyers; waitlist for the three coming-soon sub-brands.
Output: 11 home section components + composed app/page.tsx + 2 screenshots (full page + hero detail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rebuild/CONTEXT.md
@.planning/phases/03-rebuild/03-RESEARCH.md
@.planning/phases/02-design-system-review/TRANSLATION.md
@.claude/rules/design-system.md
@context/general/marketing.md
@.claude/skills/shmocard-design-system/SKILL.md
@.claude/skills/shmocard-design-system/components.css

<interfaces>
**Canonical reference (READ AND COPY — do NOT invent):**
- `.claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/Shmocard Homepage.html` — final HTML shape
- `.claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/home.css` — section layout patterns (header doc lines 1-63 lists every section)
- `.claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/home-bundle.jsx` (lines 1-300 first; reference only)
- `.claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/home-parts.jsx` (section components; reference only)
- `.claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/home-data.jsx` — `SUB_BRANDS`, `AUDIENCES`, `SHOPS`, `QUOTES`, `HOW_STEPS`, `FAQ` data shapes (translate to TS)

**Locked content (CONTEXT.md decisions):**
- B1: headline `The toolkit your crew's been [missing | asking for].` — em wraps the cycling word(s); reserve min-width from longer alternative
- B2: 10 audiences in source order: Barbers, Pawn shops, Auto shops, Jewelers, Roofers, Salons, Cafés, Restaurants, Mobile crews, Independent retailers — 38s scroll-left, paused on hover
- B3: real shop names + metrics for Proof; HARD RULE — never write "Pawn Leads" / "pawnleads" / "@pawnleads" anywhere
- B4: HOW IT WORKS 6 steps in 3-col grid: 1) You order, 2) We program, 3) Crew hands it over, 4) Customer taps, 5) Five stars, one sentence, 6) QR fallback
- B5: FAQ deferred — draft from marketing.md + product.md; Jordan reviews
- B6: Sub-brand order Review -> Biz -> Link -> Reputation; bg rotation marsh -> graham -> marsh -> cocoa; CrewStrip after Review
- B7: Footer 4 columns (already shipped 3-A3; do not touch unless link list update needed)

**Section table (TRANSLATION.md):**

| # | Section | Component | bg | nextBg (wave to) |
|---|---------|-----------|-----|------------------|
| 1 | Hero | Hero.tsx | marsh | graham |
| 2 | AudienceStrip | AudienceStrip.tsx | graham | marsh |
| 3 | Proof | Proof.tsx | marsh | marsh |
| 4 | SubBrandSpotlight Review | SubBrandSpotlight.tsx data={SUB_BRANDS[0]} | marsh | marsh |
| 5 | CrewStrip | CrewStrip.tsx | marsh | graham |
| 6 | SubBrandSpotlight Biz | SubBrandSpotlight.tsx data={SUB_BRANDS[1]} reverse | graham | marsh |
| 7 | SubBrandSpotlight Link | SubBrandSpotlight.tsx data={SUB_BRANDS[2]} | marsh | cocoa |
| 8 | SubBrandSpotlight Reputation | SubBrandSpotlight.tsx data={SUB_BRANDS[3]} reverse | cocoa | marsh (tall wave) |
| 9 | HowItWorks | HowItWorks.tsx | marsh | graham |
| 10 | Compatibility | Compatibility.tsx | graham | marsh |
| 11 | HomeFaq | HomeFaq.tsx | marsh | ember |
| 12 | FinalCta | FinalCta.tsx | ember | (footer follows) |

(Footer is global; mounted in app/layout.tsx.)

**Sub-brand data shape:**
```typescript
type SubBrand = {
  slug: 'review' | 'biz' | 'link' | 'reputation';
  number: '01' | '02' | '03' | '04';
  status: 'live' | 'soon';
  name: string;            // 'Shmo Review'
  headline: string;
  lede: string;
  bullets: string[];       // 3-5 items
  ctaPrimary: { label: string; href?: string; modal?: 'waitlist' };
  ctaSecondary?: { label: string; href: string };
  mascot: { src: string; pose: string; fitRatio?: number };
  reverse?: boolean;
  bg: 'marsh' | 'graham' | 'cocoa';
};
```

**Available assets in `public/`:**
- `/mascot/*.png` (13 emotions)
- `/products/cr80/`, `/products/l-sign/`, `/products/plate/`
- `/logo/Logo-Mascot.png`
- `/hero/Hero Image 2 - Shmo Review.png`
- Crew photos / video tiles: NOT YET available — render `<Placeholder />` muted-gray rectangles per CONTEXT.md asset table

**Motion (D-02 framer-motion):**
- `Reveal.tsx` is a `'use client'` shell wrapping `motion.div` with `initial={{opacity:0,y:16}}`, `whileInView={{opacity:1,y:0}}`, `viewport={{once:true,margin:'0px 0px -10% 0px'}}`, `transition={{duration:0.22,ease:[0.2,0.8,0.2,1]}}`. Used to wrap section roots that should reveal on scroll.
- HeroTypeCycle: `'use client'` `setInterval` cycling 2.5s; `<AnimatePresence mode='wait'>` crossfade 150ms; min-width reserved from longest alt ("asking for")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build home-data.ts + scaffold all 11 section components (skeletons)</name>
  <files>components/home/home-data.ts, components/home/home.css, components/home/Reveal.tsx, components/home/Hero.tsx, components/home/HeroTypeCycle.tsx, components/home/AudienceStrip.tsx, components/home/Proof.tsx, components/home/SubBrandSpotlight.tsx, components/home/CrewStrip.tsx, components/home/HowItWorks.tsx, components/home/VideoTestimonials.tsx, components/home/Compatibility.tsx, components/home/HomeFaq.tsx, components/home/FaqItem.tsx, components/home/FinalCta.tsx</files>
  <read_first>
    - .claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/Shmocard Homepage.html (full)
    - .claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/home.css (full — copy section layouts)
    - .claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/home-data.jsx (full — translate constants)
    - .claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/home-parts.jsx (full — section composition reference)
    - context/general/marketing.md (locked headlines + voice rules)
    - context/general/product.md (sub-brand descriptions)
    - .planning/phases/02-design-system-review/TRANSLATION.md (homepage section table)
    - components/layout/Section.tsx + Container.tsx (use these primitives)
    - components/Mascot.tsx + Sticker.tsx + mascot-poses.ts
  </read_first>
  <action>
    Create `components/home/home-data.ts` translating `home-data.jsx` constants to typed TS:
    - `SUB_BRANDS: SubBrand[4]` — Review, Biz, Link, Reputation in source order (B6)
    - `AUDIENCES: string[10]` — exactly the 10 audiences from CONTEXT B2
    - `SHOPS: { name: string; metric: string; sub: string; }[]` — real shop names + "+X% reviews" metrics; HARD RULE no "Pawn Leads" attribution
    - `QUOTES: { quote: string; attribution: string; }[]`
    - `HOW_STEPS: { n: number; title: string; body: string; }[6]` — exactly the 6 steps from CONTEXT B4
    - `FAQ: { q: string; a: string; }[]` — drafted from marketing.md + product.md; minimum 6 entries; Jordan reviews at sign-off

    Asset paths in `home-data.ts` use `/products/...`, `/mascot/...` (absolute from `public/`).

    Create `components/home/home.css` — LAYOUT ONLY. Copy the layout sections from `home.css` reference (after the section header comment block). NO primitive restyles. Comment top: `// LAYOUT-ONLY: visual lives in components.css`.

    Create `components/home/Reveal.tsx` per `<interfaces>` Reveal block. `'use client'`. Imports from `'framer-motion'`.

    Scaffold the 11 section components as server components (except HeroTypeCycle, Reveal, HomeFaq's FaqItem, AudienceStrip's marquee — see deviations below):

    Server components: Hero, AudienceStrip, Proof, SubBrandSpotlight, CrewStrip, HowItWorks, VideoTestimonials, Compatibility, HomeFaq, FinalCta.

    Client components (`'use client'`):
    - HeroTypeCycle (setInterval state)
    - FaqItem (accordion open/close)
    - VideoTestimonials' video tile click → opens VideoLightbox modal (modal stub for v1; full wiring in 03-10 alongside waitlist)

    Each section uses `<Section bg="..." nextBg="...">` from `@/components/layout/Section`. Visual classes are `.shm-*`. Layout via Tailwind utilities (`flex grid gap-* p-*`) + `home.css` module classes.

    Hero implementation:
    - Eyebrow: `<span className="shm-eyebrow">A toolkit, not a card</span>` (or per marketing.md)
    - Headline: `<h1 className="shm-display">The toolkit your crew&apos;s been <em className="shm-display__em"><HeroTypeCycle words={['missing', 'asking for']} /></em>.</h1>`
    - Lede: `<p className="shm-lede">...</p>` (drafted from marketing.md)
    - CTAs: `<a className="shm-btn shm-btn--primary">Shop Shmo Review</a>` (href=/shmo-review) + `<a className="shm-btn shm-btn--ghost">See the toolkit</a>`
    - Hand-note below: optional `<p className="shm-hand">...</p>`
    - 4-tile toolkit grid below the fold: 4 mini cards linking to each sub-brand spotlight section anchor

    SubBrandSpotlight props: `data: SubBrand`. Uses `<Mascot pose={data.mascot.pose} size="hero" fitRatio={data.mascot.fitRatio} />` (200px showcase exception). Reverse layout when `data.reverse`. CTA primary: if `data.ctaPrimary.modal === 'waitlist'`, render a button with `data-waitlist={data.slug}` (real wiring in 03-10); else regular link.

    HARD RULES throughout:
    - NO `bg-*`, `text-*` (color), `border-*` (color), `rounded-*`, `shadow-*`, `font-*` Tailwind utilities — only `.shm-*`
    - NO hex codes
    - NO emoji as decoration; NO exclamation marks
    - Mascot always via `<Mascot>` / `<Sticker>` component, never raw `<img>` for mascot art
    - String "Pawn Leads" / "pawnleads" / "@pawnleads" must NOT appear in any file produced by this task
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f components/home/home-data.ts && test -f components/home/home.css && test -f components/home/Reveal.tsx && for f in Hero HeroTypeCycle AudienceStrip Proof SubBrandSpotlight CrewStrip HowItWorks VideoTestimonials Compatibility HomeFaq FaqItem FinalCta; do test -f "components/home/$f.tsx" || (echo "MISSING $f" && exit 1); done && (grep -ri 'pawn leads\|pawnleads' components/home/ app/page.tsx; echo "PAWN_GREP_DONE") && grep -c "'use client'" components/home/HeroTypeCycle.tsx && grep -c "'use client'" components/home/Reveal.tsx && grep -c "'use client'" components/home/FaqItem.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 14 component files exist (12 sections + Reveal + FaqItem)
    - `home-data.ts` exports SUB_BRANDS (length 4), AUDIENCES (length 10), HOW_STEPS (length 6)
    - Pawn Leads grep returns ZERO matches in components/home/ and app/page.tsx
    - HeroTypeCycle, Reveal, FaqItem each contain `'use client'`; section components do NOT
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Skeleton + data + utilities ready for composition.</done>
</task>

<task type="auto">
  <name>Task 2: Compose app/page.tsx + browser-verify full homepage</name>
  <files>app/page.tsx, pictures/screenshots/03-03-homepage-full.png, pictures/screenshots/03-03-homepage-hero.png</files>
  <read_first>
    - components/home/home-data.ts (just created)
    - components/home/Hero.tsx + all 11 section files (just created)
    - components/layout/Section.tsx
    - .claude/skills/shmocard-design-system/ui_kits/website/homepage-shmocard/Shmocard Homepage.html (composition order)
  </read_first>
  <action>
    Replace `app/page.tsx` (currently the smoke test from 03-02/03-12). Compose the 12 sections in order per the section table in `<interfaces>`:

        export default function HomePage() {
          return (
            <main>
              <Hero />
              <AudienceStrip />
              <Proof />
              <SubBrandSpotlight data={SUB_BRANDS[0]} />
              <CrewStrip />
              <SubBrandSpotlight data={SUB_BRANDS[1]} reverse />
              <SubBrandSpotlight data={SUB_BRANDS[2]} />
              <SubBrandSpotlight data={SUB_BRANDS[3]} reverse />
              <HowItWorks />
              <VideoTestimonials />
              <Compatibility />
              <HomeFaq />
              <FinalCta />
            </main>
          );
        }

    Mascot inventory comment at top per RESEARCH.md "Mascot inventory" example. Document the showcase-exception 200px hero mascots (4) + the 1 sticker on FAQ.

    Run `npm run dev`. Browser-verify:
    1. Visit `/`. Confirm Nav + 11 sections + Footer render with section rotation marsh -> graham -> marsh -> ember (FinalCta) and waves between.
    2. Confirm Hero headline cycles between "missing" and "asking for" without layout shift (watch the headline; bottom of em should not move).
    3. Confirm AudienceStrip marquee scrolls left smoothly; hover pauses.
    4. Confirm all 4 sub-brands present and visually distinct.
    5. Confirm 0 console errors, 0 font 404s, 0 image 404s (placeholders are fine).
    6. Click Shmo Review CTA: takes you toward `/shmo-review` (404 OK — built in 03-04).
    7. Click "Notify me" on Biz/Link/Reputation: button reachable via tab + click; modal stub may show alert or no-op (real modal in 03-10).

    Save full-page screenshot to `pictures/screenshots/03-03-homepage-full.png`. Save above-the-fold hero screenshot to `pictures/screenshots/03-03-homepage-hero.png`.

    Final grep checks (all must return ZERO):
    - Pawn Leads brand leak: `git grep -in 'pawn leads\\|pawnleads\\|@pawnleads' app/ components/ public/`
    - Rogue Tailwind visual utilities: `grep -rnE 'className="[^"]*\\b(bg|text|border|rounded|shadow|font|animate)-[a-z]+-[0-9]' app/ components/ | grep -v 'shm-'`
    - Hex colors: `grep -rnE '#[0-9a-fA-F]{3,8}\\b' app/page.tsx components/home/`
    - Admin API: `git grep -nE 'admin\\.shopifyapis\\.com|/admin/api/' app/ components/ lib/`
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f pictures/screenshots/03-03-homepage-full.png && test -f pictures/screenshots/03-03-homepage-hero.png && grep -c 'SubBrandSpotlight' app/page.tsx && grep -c 'Hero' app/page.tsx && (git grep -in 'pawn leads\|pawnleads\|@pawnleads' app/ components/ public/ ; echo "PAWN_DONE") && (git grep -nE 'admin\.shopifyapis\.com|/admin/api/' app/ components/ lib/ ; echo "ADMIN_DONE") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Both screenshots exist
    - app/page.tsx references Hero + AudienceStrip + Proof + SubBrandSpotlight (4 calls) + CrewStrip + HowItWorks + VideoTestimonials + Compatibility + HomeFaq + FinalCta
    - Pawn Leads grep returns ZERO
    - Admin API grep returns ZERO
    - `npm run build` exits 0
    - Visual confirmation in screenshot: 4-color section rotation present, hero headline visible
  </acceptance_criteria>
  <done>Homepage complete; REQ-01 row green.</done>
</task>

</tasks>

<threat_model>
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-03-03-01 | Information disclosure | Pawn Leads brand leak in source/alt/filenames | mitigate | Pre-commit grep returns 0 for `pawn leads\|pawnleads\|@pawnleads` (HARD RULE per CONTEXT.md B3) |
| T-03-03-02 | Tampering | Sub-brand waitlist trigger button | accept | Stub only in this plan; real Server Action wiring + honeypot in plan 03-10 |
</threat_model>

<verification>
1. `npm run build` exits 0
2. Both screenshots exist; rotation + hero correct
3. All four anti-pattern grep checks return 0
4. Mascot inventory comment present
5. Hero cycle has no layout shift (visual)
</verification>

<success_criteria>
REQ-01 satisfied: homepage with 4 sub-brands equal weight, locked headline, section rotation. REQ-09 invariants pass grep.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rebuild/03-03-SUMMARY.md` recording: file list, screenshot paths, FAQ draft (for Jordan review), grep results.
</output>

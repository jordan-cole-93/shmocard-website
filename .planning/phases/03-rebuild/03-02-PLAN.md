---
phase: 03-rebuild
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - components/layout/Container.tsx
  - components/layout/Section.tsx
  - components/layout/layout.module.css
  - app/page.tsx
  - pictures/screenshots/03-02-base-layout-shell.png
autonomous: true
requirements: [REQ-09]
audit_only: partial

must_haves:
  truths:
    - "Nav.tsx renders on every page with .shm-nav primitive + 4 product links"
    - "Footer.tsx renders on every page with 4-column grid on .shm-bg-cocoa"
    - "Mascot.tsx + Sticker.tsx are typed React server components with size-prop budget enforcement"
    - "Container + Section primitives exist as reusable React components for downstream pages"
    - "Section component accepts bg prop typed as 'marsh' | 'graham' | 'ember' | 'cocoa' (4-color rotation enforced at type level)"
    - "Wave divider between sections is rendered via Section's optional nextBg prop"
  artifacts:
    - path: components/Nav.tsx
      provides: "Global nav (already shipped 3-A3)"
    - path: components/Footer.tsx
      provides: "Global footer (already shipped 3-A3)"
    - path: components/Mascot.tsx
      provides: "Typed mascot wrapper (already shipped 3-A4)"
    - path: components/Sticker.tsx
      provides: "Typed sticker wrapper (already shipped 3-A4)"
    - path: components/layout/Container.tsx
      provides: "Reusable .shm-container wrapper"
    - path: components/layout/Section.tsx
      provides: "Reusable section with bg-rotation type guard + wave divider slot"
  key_links:
    - from: app/layout.tsx
      to: components/Nav.tsx
      via: import + render
      pattern: 'import Nav from .*Nav'
    - from: app/layout.tsx
      to: components/Footer.tsx
      via: import + render
      pattern: 'import Footer from .*Footer'
---

<objective>
HYBRID AUDIT + ADD plan. Audit Nav, Footer, Mascot, Sticker (already shipped in 3-A3..3-A4). Add Container + Section layout primitives so downstream homepage / category / PDP plans can compose pages without reinventing the wrapper + section-rotation pattern.

Purpose: Single source of truth for `.shm-container` and section bg + wave divider patterns. Type-system enforcement of the 4-color rotation rule (REQ-09).
Output: Two new typed React components + audit screenshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rebuild/CONTEXT.md
@.planning/phases/03-rebuild/03-RESEARCH.md
@.planning/phases/02-design-system-review/TRANSLATION.md
@.claude/rules/design-system.md

<interfaces>
**Already shipped (verify before touching):**
- `components/Nav.tsx` — uses `.shm-nav` + 4-link product menu + cart trigger
- `components/Footer.tsx` — `.shm-bg-cocoa` 4-column grid
- `components/Mascot.tsx` — accepts `pose`, `size` ('decoration' | 'accent' | 'supporting' | 'hero'), `fitRatio`
- `components/Sticker.tsx` — accepts `pose`, `size` ('xs' | 'sm' | 'md')
- `components/mascot-poses.ts` — pose registry

**To create — interfaces:**

```typescript
// components/layout/Container.tsx
type ContainerProps = {
  children: React.ReactNode;
  className?: string;
  as?: 'div' | 'section' | 'header' | 'footer' | 'main';
};
// Renders the polymorphic element with className `shm-container ${className ?? ''}` trimmed.

// components/layout/Section.tsx
type SectionBg = 'marsh' | 'graham' | 'ember' | 'cocoa';
type SectionProps = {
  bg: SectionBg;
  nextBg?: SectionBg;
  waveSize?: 'sm' | 'md' | 'lg' | 'xl';
  containerClassName?: string;
  children: React.ReactNode;
  id?: string;
  ariaLabel?: string;
};
// Renders a section with class `shm-bg-{bg}`, an inner .shm-container,
// and (when nextBg present) a sibling div with classes
// `shm-wave shm-wave--{nextBg} shm-wave--{waveSize}` (default 'md'), aria-hidden.
```

Reference for wave classes: `context/design-system/components.css` (search `.shm-wave`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit shipped foundation components (Nav, Footer, Mascot, Sticker)</name>
  <files>components/Nav.tsx, components/Footer.tsx, components/Mascot.tsx, components/Sticker.tsx</files>
  <read_first>
    - components/Nav.tsx (full)
    - components/Footer.tsx (full)
    - components/Mascot.tsx (full)
    - components/Sticker.tsx (full)
    - components/mascot-poses.ts (full)
    - context/design-system/SKILL.md (mascot pose budget + Nav primitives section)
  </read_first>
  <action>
    Read each file. For each, confirm:
    1. Uses `.shm-*` prefixed classes for ALL visual concerns (no `bg-*`, `text-*`, `border-*`, `rounded-*`, `shadow-*` Tailwind color/visual utilities — only `flex`, `grid`, `gap-*`, `p-*` layout utilities permitted, plus CSS Modules for layout).
    2. NO hardcoded hex colors — only token references via classes or `var(--*)`.
    3. Mascot `size` prop maps to one of the four design-system size variants.
    4. Sticker `size` prop maps to one of `xs | sm | md`.

    If any violation surfaces, log in commit message — DO NOT silently rewrite the foundation work. Bug-fix only if a violation is confirmed.
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && (grep -nE 'className="[^"]*\b(bg|text|border|rounded|shadow|font|animate)-[a-z]+-[0-9]' components/Nav.tsx components/Footer.tsx components/Mascot.tsx components/Sticker.tsx | grep -v 'shm-' || echo "OK_NO_ROGUE_TAILWIND")</automated>
  </verify>
  <acceptance_criteria>
    - Grep returns "OK_NO_ROGUE_TAILWIND" or empty
    - No hex colors in any of the four files (`grep -E '#[0-9a-fA-F]{3,8}\b'` returns 0)
    - Mascot.tsx accepts only 'decoration' | 'accent' | 'supporting' | 'hero' for `size`
    - Sticker.tsx accepts only 'xs' | 'sm' | 'md' for `size`
  </acceptance_criteria>
  <done>Foundation components audited; either confirmed clean or violations fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Add Container + Section layout primitives</name>
  <files>components/layout/Container.tsx, components/layout/Section.tsx, components/layout/layout.module.css</files>
  <read_first>
    - context/design-system/components.css (search for `.shm-container`, `.shm-wave` selectors)
    - context/design-system/SKILL.md (section rotation rule)
    - .planning/phases/02-design-system-review/TRANSLATION.md (section table)
  </read_first>
  <action>
    Create `components/layout/Container.tsx` as a server component implementing `ContainerProps` from the interfaces block. Polymorphic element (default `'div'`), className is `shm-container` plus optional passed-in className.

    Create `components/layout/Section.tsx` as a server component implementing `SectionProps`. The TypeScript union `SectionBg = 'marsh' | 'graham' | 'ember' | 'cocoa'` is the type-level enforcement of the 4-color rotation rule. Markup pattern:

        <section className={`shm-bg-${bg}`} id={id} aria-label={ariaLabel}>
          <div className={`shm-container ${containerClassName ?? ''}`.trim()}>
            {children}
          </div>
          {nextBg ? <div className={`shm-wave shm-wave--${nextBg} shm-wave--${waveSize ?? 'md'}`} aria-hidden="true" /> : null}
        </section>

    `components/layout/layout.module.css` is layout-only with a top comment "Layout-only; visual lives in components.css". Empty body permitted for v1.

    Named exports: `Container`, `Section`, plus exported types `ContainerProps`, `SectionProps`, `SectionBg`. No `'use client'` — both pure server components. NO Tailwind visual utilities. NO hex.
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f components/layout/Container.tsx && test -f components/layout/Section.tsx && grep -F "'marsh' | 'graham' | 'ember' | 'cocoa'" components/layout/Section.tsx && (grep -c "'use client'" components/layout/Section.tsx | grep -q '^0$') && echo "section is server component" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - `Section.tsx` declares the type union literal `'marsh' | 'graham' | 'ember' | 'cocoa'`
    - Neither file contains `'use client'`
    - `npm run build` exits 0
    - No hex colors (`grep -E '#[0-9a-fA-F]{3,}' components/layout/*.tsx` returns 0)
  </acceptance_criteria>
  <done>Container + Section ready for downstream plans to consume.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test Section rotation in app/page.tsx + screenshot</name>
  <files>app/page.tsx, pictures/screenshots/03-02-base-layout-shell.png</files>
  <read_first>
    - app/page.tsx (current state)
    - components/layout/Section.tsx (just created)
    - components/layout/Container.tsx (just created)
  </read_first>
  <action>
    Replace `app/page.tsx` with a TEMPORARY smoke test that renders 4 stacked sections demonstrating bg rotation `marsh -> graham -> ember -> cocoa` with wave dividers between. Each section contains a `<h2 className="shm-h2">Section: {bg}</h2>` and a short body line.

    Comment at top: `// SMOKE TEST — replaced by 03-03 homepage build`. Plan 03-03 overwrites this file.

    Run `npm run dev`, load `http://localhost:3000/`, take a full-page screenshot and save to `pictures/screenshots/03-02-base-layout-shell.png`. Screenshot must show:
    - Nav at top (mounted via app/layout.tsx)
    - 4 sections visibly differentiated by background color
    - Wave dividers visible between sections
    - Footer at bottom (mounted via app/layout.tsx)
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f pictures/screenshots/03-02-base-layout-shell.png && grep -c 'bg="marsh"' app/page.tsx && grep -c 'bg="graham"' app/page.tsx && grep -c 'bg="ember"' app/page.tsx && grep -c 'bg="cocoa"' app/page.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pictures/screenshots/03-02-base-layout-shell.png` exists, shows 4 visually distinct sections in rotation order
    - `app/page.tsx` references `Section` with each of the four `bg` values at least once
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Section primitive proven working in dev; page replaceable in 03-03.</done>
</task>

</tasks>

<threat_model>none — pure presentation. No Server Actions, no Route Handlers, no user input.</threat_model>

<verification>
Phase verification per `.claude/rules/verification.md`:
1. `npm run build` returns 0
2. `pictures/screenshots/03-02-base-layout-shell.png` exists and shows 4-color rotation
3. Container + Section primitives importable from `@/components/layout/...`
4. No rogue Tailwind visual utilities in foundation files
</verification>

<success_criteria>
Layout primitives ready. Downstream pages (03-03..03-07) compose with `<Section bg="..." nextBg="...">` instead of inventing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rebuild/03-02-SUMMARY.md` recording: foundation audit findings, two new files added, smoke-test screenshot path.
</output>

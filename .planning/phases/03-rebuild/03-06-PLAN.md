---
phase: 03-rebuild
plan: 06
type: execute
wave: 3
depends_on: ["03-04", "03-12"]
files_modified:
  - components/pdp/pdp-copy.ts
  - pictures/screenshots/03-06-l-sign-pdp.png
autonomous: true
requirements: [REQ-04, REQ-06, REQ-09]

must_haves:
  truths:
    - "/shmo-review/l-sign renders the L-Sign product fetched live from Shopify (handle google-review-nfc-tap-card-l-sign)"
    - "L-Sign-specific bullets (visibility, no countertop hardware, swap during shifts) appear in PdpChecklist"
    - "Same buybox + sticky bar + add-to-cart wiring as CR-80"
    - "ZERO new component files — only pdp-copy.ts gets new entries; the route resolves via the existing [handle] dynamic route from 03-05"
  artifacts:
    - path: components/pdp/pdp-copy.ts
      provides: "L-Sign-specific bullets, subline, FAQ entries added to existing maps"
    - path: pictures/screenshots/03-06-l-sign-pdp.png
      provides: "Browser proof L-Sign PDP renders"
  key_links:
    - from: "app/shmo-review/[handle]/page.tsx"
      to: lib/shopify/queries.ts
      via: getProductByHandle('google-review-nfc-tap-card-l-sign')
      pattern: "google-review-nfc-tap-card-l-sign"
---

<objective>
Add L-Sign-specific copy entries to `pdp-copy.ts` and verify the existing dynamic PDP route from 03-05 renders the L-Sign product correctly. NO new components — the [handle] route + PDP component tree are reused.

Purpose: REQ-04.
Output: pdp-copy.ts updated + browser screenshot + smoke verification of cart-add for L-Sign variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rebuild/CONTEXT.md
@.planning/phases/03-rebuild/03-RESEARCH.md
@.claude/rules/shopify-data-discipline.md
@context/general/product.md
@context/general/marketing.md

<interfaces>
**Existing infrastructure (from 03-05):**
- `app/shmo-review/[handle]/page.tsx` already supports `urlSlug='l-sign'` via `HANDLE_MAP['l-sign'] = 'google-review-nfc-tap-card-l-sign'`. Verify in 03-05 file; if missing, FIX in 03-05 or here (but that handle MUST already be in HANDLE_MAP).
- `components/pdp/pdp-copy.ts` exports slug-keyed maps: `bullets`, `subline`, `pdpFaqFor`. Add an `'l-sign'` entry to each.

**L-Sign value props (REQ-04 + product.md):**
- Counter visibility — sits at point-of-sale, hard to ignore
- No countertop hardware — drop on the counter, no mounting required
- Swap during shifts — light enough to move; one card per active station
- Same NFC + QR fallback story as CR-80
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pdp-copy.ts with L-Sign entries + verify HANDLE_MAP</name>
  <files>components/pdp/pdp-copy.ts</files>
  <read_first>
    - components/pdp/pdp-copy.ts (current — created in 03-05)
    - app/shmo-review/[handle]/page.tsx (verify HANDLE_MAP['l-sign'] === 'google-review-nfc-tap-card-l-sign')
    - context/general/product.md (L-Sign section)
    - context/general/marketing.md (voice rules)
  </read_first>
  <action>
    1. Verify `app/shmo-review/[handle]/page.tsx` HANDLE_MAP contains `'l-sign': 'google-review-nfc-tap-card-l-sign'`. If missing, add it.

    2. Add L-Sign entries to `components/pdp/pdp-copy.ts`:
       - `bullets['l-sign']: string[]` — 4-5 items emphasizing counter visibility, no hardware, shift-swap, NFC tap, QR fallback
       - `subline('l-sign')`: short subhead under product title (e.g., "Counter-top NFC review standee")
       - `pdpFaqFor('l-sign')`: 4-6 FAQ entries — common base from `pdpFaqFor('cr-80')` PLUS L-Sign-specific items (Where do I put it? / Will it tip over? / Can I swap stations?)

    HARD RULES:
    - All copy uses voice rules from marketing.md (no exclamation marks, no emoji decoration)
    - NO "Pawn Leads" attribution
    - Marketing copy only — never claim prices (those come from Shopify variants)
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && grep -c "'l-sign'" components/pdp/pdp-copy.ts && grep -c "google-review-nfc-tap-card-l-sign" "app/shmo-review/[handle]/page.tsx" && (git grep -in 'pawn leads\|pawnleads' components/pdp/ ; echo "PAWN_DONE") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `'l-sign'` key appears in pdp-copy.ts at least 3 times (bullets, subline, FAQ map)
    - HANDLE_MAP grep returns >= 1 match for the L-Sign handle
    - Pawn Leads grep returns ZERO
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>L-Sign copy wired.</done>
</task>

<task type="auto">
  <name>Task 2: Browser-verify L-Sign PDP + cart-add smoke + screenshot</name>
  <files>pictures/screenshots/03-06-l-sign-pdp.png</files>
  <read_first>
    - app/shmo-review/[handle]/page.tsx
    - components/pdp/pdp-copy.ts (just updated)
  </read_first>
  <action>
    Run `npm run dev`. Visit `/shmo-review/l-sign`. Verify:
    1. Real L-Sign product title + price + images render (DevTools Network confirms Storefront fetch with `product:google-review-nfc-tap-card-l-sign` tag)
    2. PdpChecklist shows L-Sign-specific bullets
    3. Pack selector shows variants (if Shopify has multi-pack); qty stepper works
    4. Scroll past gallery → sticky bar appears
    5. Click Add to cart with default qty=1; confirm a POST to Storefront GraphQL endpoint in Network tab (the cart drawer wiring lands in 03-08; for now success = mutation 200 + checkoutUrl in response)
    6. ZERO console errors

    Save screenshot to `pictures/screenshots/03-06-l-sign-pdp.png`.

    Final grep checks (return 0):
    - `git grep -in 'pawn leads\\|pawnleads' components/pdp/`
    - `grep -rn 'descriptionHtml' components/pdp/`
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f pictures/screenshots/03-06-l-sign-pdp.png && (git grep -in 'pawn leads\|pawnleads' components/pdp/ ; echo "PAWN_DONE") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Screenshot exists, shows L-Sign product with L-Sign-specific bullets
    - Pawn Leads grep returns ZERO
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>REQ-04 satisfied; L-Sign PDP live.</done>
</task>

</tasks>

<threat_model>
Threats inherit from 03-05 (same component tree). No new attack surface.

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-03-06-01 | Tampering | URL handle param | mitigate | HANDLE_MAP allowlist (verified to include 'l-sign') |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `pictures/screenshots/03-06-l-sign-pdp.png` exists
3. L-Sign bullets visibly different from CR-80 in screenshot
4. Network shows fetch with l-sign handle
</verification>

<success_criteria>
REQ-04 satisfied. L-Sign reuses PDP component tree without rebuild.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rebuild/03-06-SUMMARY.md` recording: copy entries added, screenshot path, cart-add network confirmation.
</output>

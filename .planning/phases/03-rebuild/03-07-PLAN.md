---
phase: 03-rebuild
plan: 07
type: execute
wave: 3
depends_on: ["03-04", "03-12"]
files_modified:
  - components/pdp/pdp-copy.ts
  - pictures/screenshots/03-07-square-card-pdp.png
autonomous: true
requirements: [REQ-05, REQ-06, REQ-09]

must_haves:
  truths:
    - "/shmo-review/square-card renders the Square Card product fetched live from Shopify (handle google-review-plaque)"
    - "Square Card-specific bullets (mounting locations, adhesive, surface compatibility) appear in PdpChecklist"
    - "Same buybox + sticky bar + add-to-cart wiring as CR-80 / L-Sign"
    - "ZERO new component files — pdp-copy.ts gets new entries"
  artifacts:
    - path: components/pdp/pdp-copy.ts
      provides: "Square Card-specific bullets, subline, FAQ entries"
    - path: pictures/screenshots/03-07-square-card-pdp.png
      provides: "Browser proof"
  key_links:
    - from: "app/shmo-review/[handle]/page.tsx"
      to: lib/shopify/queries.ts
      via: getProductByHandle('google-review-plaque')
      pattern: "google-review-plaque"
---

<objective>
Add Square Card-specific copy entries to `pdp-copy.ts` and verify the existing dynamic PDP route renders. REQ-05.

Output: pdp-copy.ts updated + browser screenshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rebuild/CONTEXT.md
@.planning/phases/03-rebuild/03-RESEARCH.md
@.claude/rules/shopify-data-discipline.md
@context/general/product.md
@context/general/marketing.md

<interfaces>
**Existing infrastructure (from 03-05):**
- `app/shmo-review/[handle]/page.tsx` HANDLE_MAP must contain `'square-card': 'google-review-plaque'`. Verify; fix if missing.
- `components/pdp/pdp-copy.ts` exports slug-keyed maps. Add `'square-card'` entry to each.

**Square Card value props (REQ-05 + product.md):**
- Adhesive plate — mounts on surfaces (drive-thru window, dashboard, equipment)
- Mounting locations — interior or exterior; weatherproof variants
- Surface compatibility — works on glass, metal, plastic, painted drywall
- NFC + QR fallback story
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pdp-copy.ts with Square Card entries + verify HANDLE_MAP</name>
  <files>components/pdp/pdp-copy.ts</files>
  <read_first>
    - components/pdp/pdp-copy.ts (current state)
    - app/shmo-review/[handle]/page.tsx (verify HANDLE_MAP['square-card'])
    - context/general/product.md (Square Card / plate section)
    - context/general/marketing.md
  </read_first>
  <action>
    1. Verify `HANDLE_MAP['square-card'] === 'google-review-plaque'` in `app/shmo-review/[handle]/page.tsx`. Fix if missing.

    2. Add Square Card entries to `components/pdp/pdp-copy.ts`:
       - `bullets['square-card']: string[]` — 4-5 items emphasizing adhesive mount, surface compatibility, mounting locations, NFC tap, QR fallback
       - `subline('square-card')`: e.g., "Adhesive NFC review plate"
       - `pdpFaqFor('square-card')`: common base + Square Card-specific items (Will it stick to my window? / How do I remove it? / Outdoor use?)

    HARD RULES (same as 03-06):
    - Voice per marketing.md
    - No Pawn Leads
    - Marketing copy only; no hardcoded prices
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && grep -c "'square-card'" components/pdp/pdp-copy.ts && grep -c "google-review-plaque" "app/shmo-review/[handle]/page.tsx" && (git grep -in 'pawn leads\|pawnleads' components/pdp/ ; echo "PAWN_DONE") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `'square-card'` key appears in pdp-copy.ts at least 3 times
    - HANDLE_MAP grep returns >= 1 match
    - Pawn Leads grep returns ZERO
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Square Card copy wired.</done>
</task>

<task type="auto">
  <name>Task 2: Browser-verify Square Card PDP + screenshot</name>
  <files>pictures/screenshots/03-07-square-card-pdp.png</files>
  <read_first>
    - app/shmo-review/[handle]/page.tsx
    - components/pdp/pdp-copy.ts (just updated)
  </read_first>
  <action>
    Run `npm run dev`. Visit `/shmo-review/square-card`. Verify:
    1. Real Square Card product title + price + images from Shopify (Network confirms fetch tagged `product:google-review-plaque`)
    2. PdpChecklist shows Square Card-specific bullets (mounting / adhesive / surfaces)
    3. Pack selector + qty stepper functional
    4. Sticky bar slides DOWN on scroll past gallery
    5. Add to cart fires Server Action mutation (Network tab POST to Storefront)
    6. ZERO console errors

    Screenshot to `pictures/screenshots/03-07-square-card-pdp.png`.

    Grep checks (return 0):
    - `git grep -in 'pawn leads\\|pawnleads' components/pdp/`
    - `grep -rn 'descriptionHtml' components/pdp/`
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f pictures/screenshots/03-07-square-card-pdp.png && (git grep -in 'pawn leads\|pawnleads' components/pdp/ ; echo "PAWN_DONE") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Screenshot exists, shows Square Card product with Square Card-specific bullets
    - Pawn Leads grep returns ZERO
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>REQ-05 satisfied; Square Card PDP live.</done>
</task>

</tasks>

<threat_model>
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-03-07-01 | Tampering | URL handle param | mitigate | HANDLE_MAP allowlist (verified to include 'square-card') |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `pictures/screenshots/03-07-square-card-pdp.png` exists
3. Square Card bullets distinct from CR-80 / L-Sign in screenshot
</verification>

<success_criteria>
REQ-05 satisfied. All three PDPs live; PDP component tree fully reused.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rebuild/03-07-SUMMARY.md` recording: copy entries added, screenshot path.
</output>

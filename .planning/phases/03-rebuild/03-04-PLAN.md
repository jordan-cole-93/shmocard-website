---
phase: 03-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - app/shmo-review/page.tsx
  - components/category/category.css
  - components/category/CategoryHero.tsx
  - components/category/BulkMath.tsx
  - components/category/FormatCards.tsx
  - components/category/CategoryFaq.tsx
  - components/category/category-data.ts
  - pictures/screenshots/03-04-shmo-review-full.png
autonomous: true
requirements: [REQ-02, REQ-09]

must_haves:
  truths:
    - "/shmo-review renders the locked headline 'One tap. One five-star review.'"
    - "Locked tagline 'Built for crews. Priced for bulk.' appears at least once"
    - "Bulk math is visible above the fold or directly after the hero"
    - "Three format cards (CR-80, L-Sign, Square Card) link to /shmo-review/cr-80, /shmo-review/l-sign, /shmo-review/square-card respectively"
    - "Page answers 6 questions: what is this, why one per employee, how does it work, what does it cost, what do other shops say, what format do I need"
    - "Section rotation marsh -> graham -> marsh -> cocoa with wave dividers"
  artifacts:
    - path: app/shmo-review/page.tsx
      provides: "Category page composition; server component"
    - path: components/category/CategoryHero.tsx
      provides: "Headline + tagline + lede"
    - path: components/category/BulkMath.tsx
      provides: "Crew-size bulk math visualization"
    - path: components/category/FormatCards.tsx
      provides: "3 format cards linking to PDPs"
    - path: components/category/CategoryFaq.tsx
      provides: "Category FAQ"
    - path: components/category/category-data.ts
      provides: "FORMATS, BULK_MATH_TIERS, CATEGORY_FAQ data"
  key_links:
    - from: components/category/FormatCards.tsx
      to: app/shmo-review/[handle]/page.tsx
      via: Link to /shmo-review/{handle}
      pattern: "href=.*shmo-review/(cr-80|l-sign|square-card)"
---

<objective>
Build `/shmo-review` category page that sells the Shmo Review category, makes bulk math unmissable, and routes to the 3 PDPs. Deliver REQ-02.

Purpose: Visitor lands here from homepage Shmo Review CTA, sees the bulk math argument, picks a format.
Output: Category page + 4 sub-components + content data + screenshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rebuild/CONTEXT.md
@.planning/phases/03-rebuild/03-RESEARCH.md
@.claude/rules/design-system.md
@context/general/marketing.md
@context/general/product.md
@context/general/context.md

<interfaces>
**Locked content (marketing.md + REQ-02):**
- Headline (LOCKED): "One tap. One five-star review."
- Tagline (LOCKED, must appear at least once): "Built for crews. Priced for bulk."
- Six questions to answer:
  1. What is this — Shmo Review NFC review card category overview
  2. Why one per employee — bulk math hook
  3. How does it work — tap -> review flow (4-5 step explainer)
  4. What does it cost — link out to PDPs (don't hardcode prices; PDPs fetch from Shopify)
  5. What do other shops say — testimonial / proof block
  6. What format do I need — FormatCards (CR-80 wallet / L-Sign counter / Square Card adhesive)

**Three formats (linked to PDPs):**

| Format | URL | Shopify handle | One-liner |
|--------|-----|----------------|-----------|
| CR-80 | /shmo-review/cr-80 | google-reviews-nfc-tap-card-cr80 | Wallet-size card; ship one to every employee |
| L-Sign | /shmo-review/l-sign | google-review-nfc-tap-card-l-sign | Acrylic standee for the front counter |
| Square Card | /shmo-review/square-card | google-review-plaque | Adhesive plate; mounts anywhere |

**Bulk math visualization concept:**
Crew sizes (3 / 5 / 10) → cards needed → cost per review (illustrative; do NOT hardcode actual Shopify prices — say "see PDP for current pricing"). The illustrative angle: "1 card per employee = N taps per shift = X reviews per month".

**Section structure:**

| Section | bg | nextBg | Component |
|---------|-----|--------|-----------|
| CategoryHero (headline + tagline + lede + CTA "See formats") | marsh | graham | CategoryHero |
| BulkMath (crew sizes + math) | graham | marsh | BulkMath |
| FormatCards (3 cards) | marsh | marsh | FormatCards |
| HowItWorks (5-step tap flow — short version, distinct from homepage HOW IT WORKS) | marsh | cocoa | inline |
| Proof (testimonials specific to review category) | cocoa | marsh | inline |
| CategoryFaq (6-8 entries) | marsh | (footer) | CategoryFaq |

**Primitives used (.shm-*):**
- `.shm-display`, `.shm-h2`, `.shm-h3`, `.shm-eyebrow`, `.shm-lede`
- `.shm-card`, `.shm-card--soft`
- `.shm-btn--primary`, `.shm-btn--ghost`
- `.shm-faq-list` (soft)
- `.shm-image-frame` for format cards

**Reuse from homepage where applicable:**
- `<Section>` from `@/components/layout/Section`
- `<Reveal>` from `@/components/home/Reveal` (or move to a shared `components/motion/Reveal.tsx` — your call; keep import path stable)
- `<Mascot>` from `@/components/Mascot` (1 sticker on FAQ allowed; mascot pose budget = 2 max on this page)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build category-data.ts + 4 sub-components + category.css layout</name>
  <files>components/category/category-data.ts, components/category/category.css, components/category/CategoryHero.tsx, components/category/BulkMath.tsx, components/category/FormatCards.tsx, components/category/CategoryFaq.tsx</files>
  <read_first>
    - context/general/marketing.md (locked headlines + voice)
    - context/general/product.md (3 formats: CR-80, L-Sign, Square Card)
    - context/general/context.md (page intents — /shmo-review)
    - .claude/skills/shmocard-design-system/components.css (search `.shm-card`, `.shm-faq-list` for primitive shapes)
    - components/home/HomeFaq.tsx + components/home/FaqItem.tsx (reuse FaqItem pattern)
    - components/layout/Section.tsx
    - .planning/phases/02-design-system-review/TRANSLATION.md (PDP table for format card composition reference)
  </read_first>
  <action>
    Create `components/category/category-data.ts`:
    - `FORMATS: { slug: 'cr-80'|'l-sign'|'square-card'; name: string; oneLiner: string; image: string; href: string; bestFor: string; }[3]` (paths from public/products/...)
    - `BULK_MATH_TIERS: { crew: number; cards: number; tapsPerShift: string; reviewsPerMonth: string; }[]` (3-4 illustrative tiers; copy must avoid claiming exact prices)
    - `CATEGORY_FAQ: { q: string; a: string; }[]` (6-8 entries answering the 6 questions; draft from marketing.md + product.md; Jordan reviews at sign-off)

    Create `components/category/category.css` — LAYOUT ONLY. Top comment: `// LAYOUT-only; visual lives in components.css`. Empty body acceptable; add only grid/padding/aspect-ratio rules as needed.

    Create the 4 server components:

    **CategoryHero.tsx**
    - Eyebrow: `<span className="shm-eyebrow">Shmo Review</span>`
    - Headline: `<h1 className="shm-display">One tap. One five-star review.</h1>` (LOCKED — exact string)
    - Tagline: `<p className="shm-hand">Built for crews. Priced for bulk.</p>` or `<p className="shm-lede">...</p>` — LOCKED string must appear once
    - Lede: drafted from marketing.md
    - CTA primary: `<a className="shm-btn shm-btn--primary" href="#formats">See formats</a>`

    **BulkMath.tsx**
    - Heading: drafted (e.g., "1 card per employee. The math is brutal.")
    - Renders BULK_MATH_TIERS as a row of stat cards (3-col grid)
    - No actual prices — point at PDPs for current pricing ("See current pricing on each format")

    **FormatCards.tsx** — id="formats"
    - 3-col grid (mobile-stacks-to-1) of format cards
    - Each card: image (`<img>` from `/products/...`) inside `<div class="shm-image-frame">`, `.shm-h3` name, `.shm-lede` one-liner, `bestFor` line, `<Link href={`/shmo-review/${slug}`} className="shm-btn shm-btn--ghost">See details</Link>`
    - Cards link to PDP routes — slug values exactly `cr-80` / `l-sign` / `square-card`

    **CategoryFaq.tsx**
    - Reuses `<FaqItem>` from `components/home/FaqItem.tsx`
    - `<dl className="shm-faq-list shm-faq-list--soft">` wrapper
    - 6-8 questions covering the 6 question areas
    - 1 small mascot sticker via `<Sticker pose="..." size="sm" />` (within mascot pose budget)

    HARD RULES:
    - NO hex colors
    - NO Tailwind visual utilities (only `flex grid gap-* p-*`)
    - NO Pawn Leads strings anywhere
    - LOCKED headline + tagline strings exactly as specified
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && for f in CategoryHero BulkMath FormatCards CategoryFaq; do test -f "components/category/$f.tsx" || (echo "MISSING $f" && exit 1); done && test -f components/category/category-data.ts && grep -c 'FORMATS' components/category/category-data.ts && grep -c "google-review" components/category/category-data.ts && (git grep -in 'pawn leads\|pawnleads' components/category/ ; echo "PAWN_DONE") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files exist
    - category-data.ts exports FORMATS (length 3), BULK_MATH_TIERS, CATEGORY_FAQ (length 6-8)
    - Pawn Leads grep returns ZERO matches
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Category sub-components ready for composition.</done>
</task>

<task type="auto">
  <name>Task 2: Compose app/shmo-review/page.tsx + browser-verify + screenshot</name>
  <files>app/shmo-review/page.tsx, pictures/screenshots/03-04-shmo-review-full.png</files>
  <read_first>
    - components/category/CategoryHero.tsx + BulkMath.tsx + FormatCards.tsx + CategoryFaq.tsx (just created)
    - components/layout/Section.tsx
    - components/home/Reveal.tsx (motion shell)
  </read_first>
  <action>
    Create `app/shmo-review/page.tsx` as a server component composing:

        export const metadata = {
          title: 'Shmo Review — One tap. One five-star review.',
          description: 'NFC review cards built for crews. Tap to leave a Google review in seconds.',
        };

        export default function ShmoReviewPage() {
          return (
            <main>
              <Section bg="marsh" nextBg="graham"><CategoryHero /></Section>
              <Section bg="graham" nextBg="marsh"><BulkMath /></Section>
              <Section bg="marsh" nextBg="cocoa" id="formats"><FormatCards /></Section>
              <Section bg="cocoa" nextBg="marsh"><Proof inline content /></Section>
              <Section bg="marsh"><CategoryFaq /></Section>
            </main>
          );
        }

    (Adjust to the section structure in `<interfaces>`; if HowItWorks-short is wanted, insert before FormatCards.)

    Run `npm run dev`. Browser-verify at `/shmo-review`:
    1. Locked headline string "One tap. One five-star review." appears exactly
    2. Locked tagline "Built for crews. Priced for bulk." appears at least once
    3. Section rotation: marsh -> graham -> marsh -> cocoa -> marsh with waves
    4. Click each format card -> takes user toward /shmo-review/cr-80, /l-sign, /square-card (404 OK; PDPs come in 03-05/06/07)
    5. Bulk math block visible above or right after hero
    6. 0 console errors; 0 image 404s (placeholders OK)
    7. Mascot count <= 2 on this page

    Screenshot full-page to `pictures/screenshots/03-04-shmo-review-full.png`.

    Run grep checks (all return 0):
    - `git grep -in 'pawn leads\\|pawnleads' app/shmo-review/ components/category/`
    - `grep -rnE '#[0-9a-fA-F]{3,8}\\b' app/shmo-review/ components/category/`
    - `grep -rnE 'className="[^"]*\\b(bg|text|border|rounded|shadow)-[a-z]+-[0-9]' app/shmo-review/ components/category/ | grep -v 'shm-'`
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f pictures/screenshots/03-04-shmo-review-full.png && grep -c 'CategoryHero' app/shmo-review/page.tsx && grep -F "One tap. One five-star review." components/category/CategoryHero.tsx && grep -F "Built for crews. Priced for bulk." components/category/CategoryHero.tsx && (git grep -in 'pawn leads\|pawnleads' app/shmo-review/ components/category/ ; echo "PAWN_DONE") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Screenshot exists
    - Locked headline + tagline strings present in source (grep)
    - 3 format card hrefs pointing at /shmo-review/cr-80, /l-sign, /square-card
    - Pawn Leads grep returns ZERO
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>REQ-02 satisfied; category page complete; routes ready for PDP plans.</done>
</task>

</tasks>

<threat_model>
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-03-04-01 | Information disclosure | Pawn Leads brand leak | mitigate | Grep returns 0 in app/shmo-review/ components/category/ |
| T-03-04-02 | Tampering | Format card hrefs | accept | Internal Link components only; no external URLs |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `pictures/screenshots/03-04-shmo-review-full.png` exists
3. Locked headline + tagline grep returns >= 1 match each
4. All grep anti-patterns return 0
</verification>

<success_criteria>
REQ-02 satisfied. Category page sells the bulk math, hosts the 3 format cards, and points to the PDPs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rebuild/03-04-SUMMARY.md` recording: file list, screenshot path, FAQ draft, grep results.
</output>

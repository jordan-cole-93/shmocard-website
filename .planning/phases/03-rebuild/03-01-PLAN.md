---
phase: 03-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - pictures/screenshots/03-01-tokens-and-fonts.png
autonomous: true
requirements: [REQ-09]
audit_only: true

must_haves:
  truths:
    - "app/globals.css imports tailwindcss + colors_and_type.css + components.css in correct order"
    - "All four bundled fonts (Bricolage Grotesque, Cherry Bomb One, Inter Tight, Shadows Into Light Two) load and resolve via @font-face declared in colors_and_type.css"
    - "var(--color-ember), var(--color-marshmallow), var(--color-cocoa-deep), var(--color-graham) resolve to non-empty values at runtime"
    - "var(--font-display), var(--font-wordmark), var(--font-body), var(--font-hand) resolve and render correctly"
    - ".shm-bg-marsh / -graham / -ember / -cocoa flip text colors as expected on dark backgrounds"
    - "No font 404s in DevTools Network tab"
  artifacts:
    - path: app/globals.css
      provides: "Tailwind + design system import chain"
      contains: '@import "tailwindcss"'
    - path: app/globals.css
      contains: 'colors_and_type.css'
    - path: app/globals.css
      contains: 'components.css'
    - path: pictures/screenshots/03-01-tokens-and-fonts.png
      provides: "Browser proof tokens + fonts wired"
  key_links:
    - from: app/layout.tsx
      to: app/globals.css
      via: 'import "./globals.css"'
      pattern: 'import.*globals\.css'
    - from: app/globals.css
      to: context/design-system/colors_and_type.css
      via: '@import'
      pattern: 'colors_and_type\.css'
---

<objective>
**AUDIT-ONLY** plan. Verify that foundations stage 3-A1 correctly wired the design system (tokens, type ramp, all four fonts, primitives) into Next.js. Find and fix any gaps. Do **NOT** rebuild what already shipped.

Purpose: Confirm REQ-09 design-system integration is intact before homepage build (03-03) consumes these tokens.
Output: Verification screenshot + bug-fix tasks if gaps surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rebuild/CONTEXT.md
@.planning/phases/03-rebuild/03-RESEARCH.md
@.planning/phases/02-design-system-review/INTEGRATION.md
@.claude/rules/design-system.md
@.claude/rules/verification.md

<interfaces>
**Already shipped (3-A1) — verify, do NOT recreate:**

`app/globals.css` imports (in this exact order):
```css
@import "tailwindcss";
@import "../context/design-system/colors_and_type.css";
@import "../context/design-system/components.css";
```

`app/layout.tsx` imports `./globals.css`. `<Nav />` and `<Footer />` mount globally.

Tokens that MUST resolve (from `context/design-system/colors_and_type.css`):
- Colors: `--color-ember`, `--color-marshmallow`, `--color-graham`, `--color-cocoa-deep`, `--color-cream`, `--color-honey`, `--color-cherry`, `--color-clover`, `--color-ink-1`..`--color-ink-3`
- Fonts: `--font-display` (Bricolage Grotesque), `--font-wordmark` (Cherry Bomb One), `--font-body` (Inter Tight), `--font-hand` (Shadows Into Light Two)
- Motion: `--motion-fast` 150ms, `--motion-base` 220ms, `--motion-slow` 320ms, `--ease-standard`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit globals.css + layout.tsx import chain</name>
  <files>app/globals.css, app/layout.tsx</files>
  <read_first>
    - app/globals.css (current state — already shipped 3-A1)
    - app/layout.tsx (current state — already shipped 3-A1)
    - context/design-system/colors_and_type.css (top 100 lines for @font-face + token list)
    - context/design-system/components.css (top 50 lines)
    - .planning/phases/02-design-system-review/INTEGRATION.md
  </read_first>
  <action>
    Verify the file states match the locked Option A integration strategy from INTEGRATION.md:

    1. `app/globals.css` MUST contain these imports IN THIS ORDER:
       - `@import "tailwindcss";`
       - `@import "../context/design-system/colors_and_type.css";`
       - `@import "../context/design-system/components.css";`
    2. `app/layout.tsx` MUST `import "./globals.css"` at the top of the file.
    3. NO duplicate `@theme` block transcribing tokens (Option B was rejected per D-04 in INTEGRATION.md).
    4. NO Tailwind utilities are restyling visual surfaces (visual = `.shm-*`).

    If any check fails, fix it inline (this is the only legitimate code change in this audit plan). If everything passes, write nothing — just record in commit message that audit passed.

    Reference INTEGRATION.md Option A (locked) for the canonical wiring.
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && grep -c '@import "tailwindcss"' app/globals.css && grep -c 'colors_and_type.css' app/globals.css && grep -c 'components.css' app/globals.css && grep -c 'import "./globals.css"' app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@import "tailwindcss"' app/globals.css` returns 1
    - `grep -c 'colors_and_type.css' app/globals.css` returns 1
    - `grep -c 'components.css' app/globals.css` returns 1
    - `grep -c 'import "./globals.css"' app/layout.tsx` returns 1
    - No `@theme` block in app/globals.css (`grep -c '@theme' app/globals.css` returns 0)
  </acceptance_criteria>
  <done>Import chain verified intact OR fixed to match Option A.</done>
</task>

<task type="auto">
  <name>Task 2: Browser-verify tokens + fonts resolve in dev server</name>
  <files>pictures/screenshots/03-01-tokens-and-fonts.png</files>
  <read_first>
    - context/design-system/colors_and_type.css (token list + @font-face declarations)
    - context/design-system/preview/*.html (existing preview cards as reference for what working tokens look like)
  </read_first>
  <action>
    Run `npm run dev` then load `http://localhost:3000/`. Open DevTools.

    Verify in DevTools Console (run these expressions):
    ```js
    getComputedStyle(document.documentElement).getPropertyValue('--color-ember')
    getComputedStyle(document.documentElement).getPropertyValue('--color-marshmallow')
    getComputedStyle(document.documentElement).getPropertyValue('--color-cocoa-deep')
    getComputedStyle(document.documentElement).getPropertyValue('--font-display')
    getComputedStyle(document.documentElement).getPropertyValue('--font-wordmark')
    getComputedStyle(document.documentElement).getPropertyValue('--font-body')
    getComputedStyle(document.documentElement).getPropertyValue('--font-hand')
    ```
    Each MUST return a non-empty value.

    Verify in DevTools Network tab (filter: Fonts):
    - All four font families load with HTTP 200 (Bricolage variable, Cherry Bomb One, Inter Tight upright + italic, Shadows Into Light Two)
    - ZERO font 404s

    Take a full-page screenshot via Playwright (or browser DevTools "Capture full size screenshot") and save to `pictures/screenshots/03-01-tokens-and-fonts.png`. The screenshot must clearly show the Nav (Cherry Bomb wordmark + Bricolage links) rendered in their correct fonts, NOT in fallback system-ui.

    If any token returns empty OR any font 404s, this is a regression — fix the @import chain or @font-face paths in `colors_and_type.css` BEFORE proceeding. (Hint: check the relative path resolves from `app/globals.css` location.)
  </action>
  <verify>
    <automated>cd "/Users/jordancole/Documents/Developement/Projects/Shmocard/Shmo Website" && test -f pictures/screenshots/03-01-tokens-and-fonts.png && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pictures/screenshots/03-01-tokens-and-fonts.png` exists and shows wordmark in Cherry Bomb (round bouncy letters), nav links in Bricolage (geometric grotesque)
    - All seven CSS variables in the action above return non-empty strings
    - Network tab shows zero font 404s
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Screenshot saved + tokens + fonts confirmed live. REQ-09 foundation row green.</done>
</task>

</tasks>

<threat_model>none — pure presentation / static asset wiring. No Server Actions, no Route Handlers, no user input.</threat_model>

<verification>
Phase verification per `.claude/rules/verification.md`:
1. `npm run build` returns 0
2. `pictures/screenshots/03-01-tokens-and-fonts.png` exists
3. Console expressions return non-empty values
4. No font 404s
</verification>

<success_criteria>
Foundations 3-A1 wiring confirmed intact. Design system available for all downstream plans (03-02..03-12).
</success_criteria>

<output>
After completion, create `.planning/phases/03-rebuild/03-01-SUMMARY.md` recording: audit findings (PASS / fixes applied), screenshot path, console-resolved token values.
</output>
